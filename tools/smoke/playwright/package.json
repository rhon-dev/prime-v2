{
  "name": "primev2-staging-smoke",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "scripts": {
    "test": "npx playwright test --reporter=list",
    "install-playwright-browsers": "npx playwright install --with-deps"
  }
}
